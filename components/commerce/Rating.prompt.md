Read-only star rating using Everyday-Blue stars, with optional numeric value and review count.

```jsx
<Rating value={4.5} count={1284} />
<Rating value={4} size={20} showValue />
```

Props: `value` (0–5, halves), `count`, `size`, `showValue`.
