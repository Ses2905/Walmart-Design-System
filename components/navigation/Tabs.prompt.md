Underline tab bar; active tab is navy text with a True-Blue underline, optional count pills.

```jsx
<Tabs
  tabs={[{id:'all',label:'All'},{id:'active',label:'Active'},{id:'attention',label:'Needs attention',count:3}]}
  defaultValue="all"
  onChange={id => {}}
/>
```
