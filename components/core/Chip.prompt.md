Pill chip for filters, facets, and applied-filter tokens; navy fill when selected.

```jsx
<Chip selected icon="filter">Under $500 budget</Chip>
<Chip onRemove={() => {}}>Back to School</Chip>
```

Props: `selected`, `icon`, `onClick`, `onRemove` (shows close ×).
