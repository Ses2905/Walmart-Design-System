Rounded-square checkbox that fills True Blue with a checkmark when on. Controlled or uncontrolled.

```jsx
<Checkbox label="Text me budget alerts" defaultChecked />
<Checkbox label="Auto-bidding only" checked={v} onChange={e => setV(e.target.checked)} />
```
