Rounded-square checkbox that fills True Blue with a checkmark when on. Controlled or uncontrolled.

```jsx
<Checkbox label="Free shipping" defaultChecked />
<Checkbox label="In stock only" checked={v} onChange={e => setV(e.target.checked)} />
```
