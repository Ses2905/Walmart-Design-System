White, softly-rounded surface container for content modules.

```jsx
<Card elevated padding={24}>…</Card>
<Card interactive>Clickable module</Card>
```

Props: `padding`, `interactive` (hover lift), `elevated` (resting shadow).
